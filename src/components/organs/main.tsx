import React, { Component } from "react";
import RegistForm from "./registForm";

const Main: React.FC = () => {
	return (
		<main>
			<article>
				<RegistForm />
			</article>
		</main>
	);
};

export default Main;
